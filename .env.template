AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_MODEL=gpt-4o
AZURE_OPENAI_MODEL_VERSION=2024-12-01-preview

# Comma-separated browser origins allowed to call the API (CORS), e.g. http://localhost:5173
# Leave empty to use built-in dev defaults (Vite :5173, Docker UI :8080, API :8000).
CORS_ORIGINS=

# Frontend (optional): frontend/.env.local with VITE_API_BASE_URL=... for local dev CORS.
